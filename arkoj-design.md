# ArkOJ 设计纲要（v0.1）

## 1. 系统构成
| 模块 | 说明 |
|---|---|
| 题库 | 题目、标签、难度、题面(Markdown/LaTeX)、测试数据、SPJ/交互题 |
| 提交与评测 | 编辑器、多语言、队列、评测机(编译→沙箱→校验)、状态回流 |
| 用户体系 | 注册/登录、权限、主页、提交历史、Rating |
| 比赛 | 时间窗、ACM/OI 赛制、封榜、补题 |
| 排行榜 | 静态解题榜 + 动态 Rating |
| 公告/讨论/后台 | 平台信息与运维 |

## 2. 主页信息架构（以用户为主语）
1. 问候区（Hero）
   - 未登录：`欢迎来到ArkOJ` / `算法竞赛人`
   - 已登录：`[时段]好` / `[用户名]`，附实时时钟与个人数据
2. 用户闭环卡片：继续挑战 → 每日一题 → 最近提交
3. 驱动卡片：赛事日程 / 排行 / 公告
4. 未登录转化卡片：快速开始 01-02-03 / 热门题目 / 平台状态 / 关于
5. 页脚：条码 + 坐标微文字

## 3. 视觉令牌
- 主题色 `#9DC1F1`（工程蓝，替代传统警示黄作为主高亮）
- 警示红 `#E0433D`（仅用于关键状态/进行中直播点）
- 炭黑 `#16181D`，工业白 `#F4F5F7`，冷灰线 `#D5DAE3`
- AC 绿 `#2E9E6B` / TLE 琥珀 `#E3A63B`
- 字体：极粗无衬线标题（紧字距）+ 等宽微文字（`//`、`[ ]`、坐标、版本号）
- 元素：等高线背景、细网格、四角 HUD 括号、十字准星、45° 切角、
  信号方块序列、条形码、轻微 RGB 色散与半色调网点

## 4. 时段问候映射
| 时间 | 问候 |
|---|---|
| 00–05 | 深夜好 |
| 05–09 | 早上好 |
| 09–12 | 上午好 |
| 12–14 | 中午好 |
| 14–18 | 下午好 |
| 18–23 | 晚上好 |
| 23–24 | 深夜好 |

## 5. 页面清单（一期）
`/` 主页 · `/problems` 题库 · `/problem/:id` 题目详情 · `/submissions` 提交记录 ·
`/contests` 比赛 · `/contest/:id` 比赛内页 · `/rank` 排行 · `/discuss` 讨论 ·
`/user/:id` 用户主页 · `/settings` 设置 · `/admin` 后台

## 6. 设计语言扩展（v0.2）
### 切角三式（可叠加）
- a. 缺口补色：切角处填一个异色三角形（如蓝块 + 深蓝角）
- b. 分离放大：角落三角形与矩形脱离并放大
- c. 漂浮错位：新增三角形，偏移坐标、略微放大，悬于主体之外
### 线条解构
- 线条不必连贯：可以是渐隐渐变、边角点缀、与主体错位
- 拼接条：一条线由多段异色短条拼成（含纵向错位）
### 深色模式
- class 策略 `html.dark`，令牌整体翻转（见 index.css `.dark`）
- 唯一不翻转的语义令牌：`--color-on-accent`（高亮块上的深色文字）
- 主题偏好持久化 localStorage `arkoj-theme`，默认跟随系统
